import 'package:flutter/material.dart';

class MateComunicacion extends StatelessWidget {
  const MateComunicacion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Comunicacion Digital'),
        backgroundColor: Colors.black38,
      ),
    );
  }
}