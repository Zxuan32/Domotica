import 'package:flutter/material.dart';

class MateDispositivos extends StatelessWidget {
  const MateDispositivos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dispositivos Autonomos'),
        backgroundColor: Colors.black38,
      ),
    );
  }
}